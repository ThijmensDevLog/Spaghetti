# Spaghetti

The future of automation!


## Usage
1. Clone this repository `git clone https://github.com/ThijmensDevLog/Spaghetti.git` 
2. Get into the directory `cd Spaghetti`
3. Get into the backend directory `cd Backend`
4. Run the backend `uvicorn main:app --reload `